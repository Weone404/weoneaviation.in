# Weekly Growth Report — 2026-W39 (run of 2026-09-26)

## 1. Verdict

**SHIPPED** (pending this run's merge + push to `master`; see commit log for
the exact SHA — this is the first run under the structured `docs/` process,
so there is no prior week's SHA to diff against). No rollback needed.

## 2. Citation scoreboard

First run — no prior week to diff against. Full detail with sources in
`docs/geo-knowledge-base.md` § 2026-W39. Summary:

| Prompt | Cited? | Winner |
|---|---|---|
| best DGCA ground classes in Delhi | No | Golden Epaulettes Aviation |
| pilot training institute in Dwarka Delhi | No | Airborne Aviation / Golden Epaulettes |
| how to become a pilot in India after 12th | No (no institute cited) | — open field |
| CPL training cost in India 2026 | No | Golden Epaulettes / Airborne Aviation |
| DGCA ground classes fees Delhi | No | Golden Epaulettes / VFTI |
| which institute for DGCA prep in Delhi | No | Golden Epaulettes / CSKAA |
| We One Aviation reviews | N/A — surfaced **weoneaviation.com** (out of scope) making unverifiable claims | — |
| is We One Aviation DGCA approved | **Yes** | our own /about-us, /pilot-training-in-india |

**Reading**: brand-query visibility is fine; unbranded local-commercial-intent
visibility is the gap, and it's the same competitor (Golden Epaulettes
Aviation) across nearly every query. This should drive next week's CONTENT &
AEO rotation slot.

## 3. What changed

### P0

- **Removed fabricated/unverifiable claims live on the homepage and other
  high-traffic pages.** `pages/index.jsx`, `pages/pilot-training-in-india.jsx`,
  `pages/courses.jsx`, `pages/air-navigation.jsx`,
  `pages/airline-preparation-course.jsx`, `pages/best-flight-schools-in-usa.jsx`,
  `pages/flying-school/usa.jsx`. Specifics: "guaranteed instructor job
  opportunities" / "direct airline placement programs" / "international
  pilot job opportunities"; "Hundreds of successful pilots flying with
  leading airlines" under "Proven Track Record of Success" (duplicated
  across two pages); "We Provide 100% Loan For Flight Training From Any
  Country" (duplicated); a "Topper Scholarship Program" offering free CPL
  training under a "Money-Back Guarantee" with no documented terms anywhere
  in the repo (removed, not hedged); "guaranteed career guidance" plus a
  claim of training "with state-of-the-art aircraft" (contradicts the site's
  own established "we do not own aircraft or simulators" position);
  "Guaranteed Results" with an implied but unstated pass rate; an "Alumni
  Success Stories" testimonial-style claim with no named/documented alumni;
  and interview-prep copy claiming to "assist students in securing job
  opportunities with leading airlines," contradicting the site's own stated
  scope used correctly everywhere else. Also removed two near-identical
  "40-Hour PPL Challenge" / free-Instrument-Rating / fake "Limited Seats"
  urgency claims from the two USA training pages, which turned out to be
  byte-identical in body copy (see P2 below).
- **Extended the automated claims gate** (`scripts/check-claims.js`) with 7
  new patterns so this exact class of claim — prose-form guarantee/
  placement/success language without a specific percentage or headcount —
  fails the build if it recurs. The gate had zero coverage for it before
  this run.

### P1

- Fixed a real 2-hop redirect chain (`/ecga-login-your-compl` →
  `/ecga-login-your-complete-guide` → `/egca-login`, plus a second legacy
  source pointing at the same intermediate) and its 404 destination —
  collapsed both to a single hop.
- Deleted two page files permanently unreachable due to their own
  `next.config.js` redirects (`pages/faq.jsx`, confirmed live via a running
  build that `/faq` 308s to `/faqs`; `pages/pilot-course-training-in-india.jsx`,
  confirmed `/pilot-course-training-in-india` 308s to `/pilot-training-in-india`).
  Zero internal references to either before deletion.
- Added 30 missing real, live URLs to `public/llms.txt` and removed one dead
  legacy entry (`/blogs/4`, which now only redirects) — restored llms.txt /
  sitemap parity except one intentional canonical exception (documented).
- Fixed 3 `@next/next/no-html-link-for-pages` lint errors (raw `<a>` instead
  of `<Link>`) — 2 were in the deleted orphan pages, 1 in
  `pages/commercial-pilot-license.jsx`.
- Added an accessible name to the shared course `<select>` in
  `components/LeadForm.jsx` (used site-wide) — fixed a Lighthouse
  `select-name` accessibility failure everywhere the component appears in
  one change.

### P2

- Fixed a false-positive "duplicate EducationalOrganization schema" flag in
  `geo-qa.js` (was counting nested `publisher:`/`provider:` references —
  the schema.org-recommended shape — as top-level duplicates). No page
  schema was actually broken; this was a tooling fix.
- Found (via a duplicate-meta-description check) that
  `pages/best-flight-schools-in-usa.jsx` and `pages/flying-school/usa.jsx`
  were byte-identical in body copy. Both de-duplicated and differentiated in
  place this run (distinct H1/title/meta, distinct quick-facts and sidebar
  content) rather than 301-consolidated, to keep both keyword targets;
  logged to backlog to decide fully differentiating vs. consolidating in a
  future content rotation.
- Wired `verify:links`, `verify:schema`, `verify:meta`, `audit:crawl`, and
  `verify:all` into `package.json`, reusing existing audit scripts
  (`scripts/audit-links.cjs`, `geo-qa.js`, `scripts/seo-audit.js`) rather
  than rewriting them from scratch. Added `scripts/audit-crawl.mjs` (new —
  none of the existing scripts produced the Phase 1 per-URL inventory this
  routine's process calls for).

### Found, not fixed this run (see backlog)

- Sitewide `color-contrast` Lighthouse failures — systemic, needs a real
  design-token pass with visual verification; too large to rush safely this
  run. Logged with the exact shared classes involved for a fast start next
  Design/Perf rotation.
- `heading-order` skips (h2→h4, no h3) in a hand-duplicated "Eligibility
  Criteria" sidebar box pattern across 9 files.
- Entity-authority / AI-citation gap (see scoreboard above) — the real
  driver for next week's rotation.

## 4. Verification results

| Check | Result |
|---|---|
| `npm ci` | Clean |
| `npm run lint` | 0 errors / 0 warnings in every file this run touched. 5 pre-existing errors + 5 warnings remain in `pages/admin/*` and `utils/emailService.js` — not touched this run, out of scope per CLAUDE.md (blog-routine carve-out applies to the same pre-existing set). |
| `npm run check:claims` | Clean (289 files scanned) after fixes; 2 non-blocking vocabulary warnings (`navigate` ×9, `landscape` ×1) — reviewed, both are literal/technical uses (aircraft navigation, a `landscape`-orientation CSS context), not marketing filler. |
| `npm run build` | Succeeds, zero warnings introduced. |
| `npm run verify:links` | 0 broken internal links. 2 flagged items are known tooling false positives (`/favicon.ico` — a real static file the script doesn't check; `/pdfs` — a documented graceful-degradation path with no `public/pdfs/` by design). Orphans/single-inbound-link findings are all pre-existing blog-content pages, not touched this run. |
| `npm run verify:schema` (`geo-qa.js`) | 22 failures remain, all inside `pages/blogs/*.jsx` content (medical-class-split / DGCA-subject-count figures against `lib/facts.js`, missing route-specific FAQs on recent posts) — the daily blog routine's domain, logged to backlog rather than edited here, per this routine's rule never to touch that pipeline's content. |
| `npm run verify:meta` (`scripts/seo-audit.js`) | Clean — no duplicate titles/descriptions (the one pair found mid-run was fixed; see P2 above). |
| `npm start` + `npm run audit:crawl` | 114 routes crawled, 0 server errors, 0 pages with >1 `<h1>`; the only 2 pages missing `<title>`/`<h1>` are `/robots.txt` and `/sitemap.xml` (non-HTML routes — expected). Full inventory: `docs/audit/inventory-2026-W39.json`. |
| Diff review (`git diff master`) | No `www.` or `.com` self-reference, no `http://`, no secrets, homepage `<h1>` intact (exactly 1, confirmed live), `SITE_URL` still apex. |
| Lighthouse (desktop, 4 key routes) | `/`: Perf 100, A11y 97, BP 96, SEO 92, LCP 0.7s, CLS 0, TBT 0ms. `/commercial-pilot-license`: Perf 100, A11y 96, BP 96, SEO 100. `/dgca-ground-classes`: Perf 100, A11y 95, BP 96, SEO 100. `/pilot-training-in-dwarka`: Perf 100, A11y 95, BP 96, SEO 100. No prior week's numbers to diff against (first run). Remaining gaps: sitewide `color-contrast` (all 4 pages), `heading-order` (2 of 4), `errors-in-console` (all 4 — see below), homepage-only `link-text` ("Learn More" links, not fixed this run — logged). |
| Mobile Lighthouse, full 8-route sweep, axe-core, keyboard-only pass, responsive screenshot sweep | **Not run this run** — out of time budget after the P0 claims discovery expanded scope. Flagging explicitly rather than claiming a pass: this routine's own rule is to say so rather than claim success when a check can't be completed. |
| Console errors | 2 network failures on every page (`googletagmanager.com`, a `tecmicra.com` webhook) — almost certainly this sandboxed environment's outbound proxy blocking those hosts, not a live defect (neither host is referenced in this repo's own source). Needs re-verification against the live site post-deploy. |
| Forms | Not independently re-tested this run; no form-handling code was touched. |

## 5. Ecosystem changes this week

See `docs/geo-knowledge-base.md` for full detail and sources. Highlights: no
new Google core update since the August 2026 spam update; AI Mode features
are merging into AI Overviews (dynamic expansion, Generative UI, link
carousels); Google's FAQ rich-result snippet is deprecated in classic Search
(since May 2026) but FAQPage schema itself is still valid and still read by
AI answer engines — keep shipping it; schema.org v30 shipped stable in
March 2026 with no breaking changes for this site's types; DGCA's Regular
Exam 03 of 2026 runs this week (Sept 22–26).

## 6. Owner actions (off-site)

- **weoneaviation.com** (out of scope, cannot be touched by this routine)
  is surfacing in web search results describing the academy as "trusted by
  over 6000 candidates with 4000+ placements" and naming an instructor with
  a specific first-attempt pass count — exactly the claim types this
  routine strips from weoneaviation.in. Recommend the owner review and align
  it, or take it down if unmaintained.
- Google Business Profile access wasn't available this run — compare GBP
  footprint against Golden Epaulettes / CSKAA next time it's available; ties
  directly to the citation gap above.
- No third-party backlinks to weoneaviation.in surfaced in any of the six
  unbranded prompts probed this week — outreach is outside this routine's
  scope but would likely move the needle faster than on-site work alone.

## 7. Backlog delta

Added this run: sitewide color-contrast pass (P1), heading-order fixes in 9
files (P2), console-error re-verification post-deploy (P3), entity-authority
content rotation target (P1, carries into next week), USA-pages
consolidation decision (P2), babel-preset / useRouter-stub smoke-test notes
(P3), CPL/DGCA cost-figure freshness check against 2026 market data (P2).
Cleared this run: the full P0 claims list above, the redirect chain, the two
orphan pages, the llms.txt parity gap, the false-positive schema-dup check,
the lint errors, and the select-name accessibility fix. Full detail in
`docs/weekly-backlog.md`.

## 8. Next week's plan

ISO week 40 → rotation slot **CONTENT & AEO** (week % 4 == 1). Top 5 targets,
driven directly by this week's citation scoreboard:

1. Rewrite the weakest commercial pages (starting with whichever of
   `/dgca-ground-classes`, `/pilot-training-in-dwarka`,
   `/commercial-pilot-license-admission-process` scores lowest on fact
   density / answer-first structure) to directly answer the exact phrasing
   of the six unbranded prompts that returned no citation this week.
2. Verify the entity-clarity paragraph ("We One Aviation is a DGCA-approved
   pilot training institute in Dwarka, New Delhi, operating since 2009…")
   appears verbatim on every page that should carry it.
3. Check `/cost-transparency` and `lib/facts.js` PARIKSHA figures against
   the 2026 market data found this week (₹55L–90L CPL total,
   ₹80k–5L DGCA ground-class fees) — source before changing any number.
4. Decide and execute on the best-flight-schools-in-usa /
   flying-school/usa duplicate-content question (differentiate further or
   301-consolidate).
5. Fix the 9-file heading-order issue and the homepage `link-text` finding
   (both quick, both currently logged) as low-risk filler work alongside the
   content rotation.
